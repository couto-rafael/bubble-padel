# Context Dump — Bubble Padel — Sprint 8.5 ENCERRADA — 2026-06-10

- **Stack & Deploy:** React+TS+Tailwind+Vite (`projeto_novo/src/`, porta 5173) · Node+Express+Prisma+PostgreSQL (`backend/src/`) · Neon (pooler 6543) · Railway Hobby auto-deploy via git push master · Repo: github.com/couto-rafael/bubble-padel · Branch master · Workflow: Claude.ai = planning/Gate0-1, Claude Code = execução · Services: AbacatePay (PIX), Resend, Sentry, UptimeRobot, Cloudinary (signed upload). Pre-commit hook Husky ativo: `tsc --noEmit` em backend/ e projeto_novo/.

- **Sprint 8.5 status:** ✅ TOTALMENTE ENCERRADA. Todos smoke tests 4-10 validados em runtime no torneio "Smoke Test Regular" (id `cmp7qjg6i0001148ikschzbjv`) + Super 8 (id `cmq8webbg0001xcx5ge29lx2a`). MATCH_RESULT, TROPHY, syncStatus idempotente, idempotência placar, tabs, BottomNav resiliente — todos passaram. 11/12 TROPHY posts (1 privado, comportamento correto). Próximo: 🔄 Sprint 9 — Feed Social v1 (A) + Card enriquecido (B) + Nav atleta (C). Bugs críticos parqueados pra Sprint 10 (hardening pré-lançamento).

- **Bugs fixados nesta sessão (commits master):** `a806a36` perfil/feed com res.ok + AthleteBottomNav resiliente · `21d8c57` parse de brackets em seed-tournament-scores · fix TROPHY no syncStatus manual (generateTrophyPosts exportado, chamada fire-and-forget em tournaments.ts ~436) · `3dc9819` BACKLOG limpo de itens concluídos · `ef0c6be`+`3447a16` BACKLOG com 3 achados de UX + bug Super8 sem posts · `198f982` BACKLOG adicionado bug Super8 sem auto-posts.

- **Bugs parqueados no BACKLOG (Sprint 10 — hardening pré-lançamento):**
  - **Crítico:** Super8Match não dispara MATCH_RESULT post — implementar `maybeCreateSuper8MatchResultPost` adaptando lógica (4 atletas únicos por partida, parceiros rotativos)
  - **Alta:** Race 401 mount em `/athlete/profile|tournaments|stats` (reproduzido em prod) · Validação de gênero por categoria (bloquear inscrição cross-gender exceto Mista, requer backfill Athlete.gender)
  - **Média:** Card MATCH_RESULT no feed sem parceiro/adversários/fase · Nav atleta — único caminho pro dashboard é o logo · Página notificações do clube (NotificationsPage hoje é só atleta)
  - **Baixa:** Classificação final ausente no perfil · seed-tournament-scores single-pass (4 runs manuais)

- **Absolute rules (não mudaram):** Nunca `prisma migrate dev`, sempre `npx prisma db push` + `npx prisma generate` · Imports relativos (`../`), nunca `@/` · Status enums backend MAIÚSCULO, frontend `.toLowerCase()` · Frontend folder `projeto_novo/` · `auth_user` localStorage tem campo `type` valores `CLUB`/`ATHLETE` · Login/logout sempre via `useAuth()`, nunca `AuthService` direto · Transação Prisma com Neon: `{ timeout: 30000 }` quando 3+ creates sequenciais · Banco dev = produção (mesmo Neon).

- **Estado dos dados em prod:** 44 atletas (3 reais + 41 seeds) · Friendships ACCEPTED: A↔B real + A↔seed.d + A↔seed.e · Torneio Smoke Test Regular COMPLETED (42 duplas, 3 cats, 11 TROPHY posts + 6 MATCH_RESULT) · Torneio Smoke Test Super 8 COMPLETED (8 atletas, 14 matches, 0 posts) · Clube: `rafaelcouto.ca@gmail.com` (Joinville Padel, id `cmohy2hav0001we31hx53pxvb`) · Senha em `backend/.env` como `CLUB_PASSWORD`.

- **Achados arquiteturais úteis pra Sprint 9:** Tournament.tournamentType (string) — valores "Super 8" | "Regular (Grupo + Playoffs)" | "Eliminatorias Diretas" · Super 8 usa modelo separado `Super8Match` (round + matchIndex + 4 player names, sem groupId/teamId) · Endpoint próprio PUT `/api/super8/:tid/matches/:mid` · `maybeCreateMatchResultPost` (Match) + `maybeCreatePlayoffMatchResultPost` (PlayoffMatch) — Super8 não tem equivalente · `generateTrophyPosts` em `statusSync.ts` (agora exportado, idempotente via metadata.tournamentId, gate de privacy settings.matches=PRIVATE).
