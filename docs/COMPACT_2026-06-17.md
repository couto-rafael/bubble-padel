Context Dump — Bubble Padel — Sprint 9 ENCERRADA — 2026-06-17

Stack & Deploy: React+TS+Tailwind+Vite (projeto_novo/src/, porta 5173) · Node+Express+Prisma+PostgreSQL (backend/src/) · Neon (pooler 6543) · Railway Hobby auto-deploy via git push master · Repo: github.com/couto-rafael/bubble-padel · Branch master · Workflow: Claude.ai = planning/Gate0-1 (prompts enxutos sem código completo), Claude Code = execução. Pre-commit hook Husky: tsc --noEmit em backend/ + projeto_novo/. Workflow refinado mid-sprint: 1 prompt único por turno pro Code (não múltiplos parallel), prompts enxutos (regras + endpoints + arquivos + commit — sem código completo nem rationale).
Sprint 9 status: ✅ ENCERRADA. Entregas: C (nav atleta "Meu Dashboard"), A0 (schema PostLike+PostComment+mentions), A1 (composer manual), A2 (likes idempotente), A3 (comments flat), A4 (@mentions com autocomplete + render), A5 (share Web API), B1 (MATCH_RESULT enriquecido com partner/opponents/phase), B2 (TROPHY visual com badge CAMPEÃO/VICE). Fixes mid-sprint: casing user.type === "ATHLETE" (cast as string), single-word mention token (fullName.split(" ")[0]), remoção do gate hasFriendInMatch (atleta sempre recebe próprio post), dedup MATCH_RESULT por matchId no GET /feed. Smoke headless + visual validado.
Commits Sprint 9 master (em ordem):

3c83b86 feat(nav): "Meu Dashboard" dropdown · 3ee8466 feat(schema): PostLike+PostComment+mentions
8ea8177 feat(feed): composer · 1acc615 feat(feed): likes · 377e438 feat(feed): comments
2e62c1d feat(feed): @mentions · 9c74b6f fix(feed): single-word token
9a1675f feat(feed): share · be385f2 chore(backlog): mention polish parking
4a8b9b4 feat(feed): MATCH_RESULT enriquecido · 9332725 feat(feed): TROPHY visual
de2b0c8 chore: gitignore .claude/settings.local.json · c00362e chore(backlog): AthleteProfileById back nav
03ef334 chore(backlog): "Iniciar Torneio" button · 073df7e chore(backlog): score display inconsistency
2ad4631 fix(feed): remove hasFriendInMatch gate · 027f1b1 fix(feed): dedup by matchId
ce59fe5 chore(backlog): 3 playoff bugs B1 smoke

Schema mudanças (Sprint 9): AthletePost.mentionedAthleteIds String[] @default([]) · novo model PostLike(postId, athleteId) com @@unique([postId, athleteId]) + index athleteId · novo model PostComment(postId, athleteId, content, mentionedAthleteIds[], createdAt, updatedAt) flat sem threading · Athlete ganhou relations postLikes + postComments. Aplicado via npx prisma db push (NUNCA migrate dev). Mudança aditiva — posts antigos intactos.
Endpoints Sprint 9 (todos em backend/src/routes/feed.ts exceto search):

POST /api/athlete/posts — composer manual, body {content 1-2000, mentionedAthleteIds?[]}, valida ids existem
POST /api/athlete/posts/:id/like — upsert idempotente, retorna {liked, likeCount}
DELETE /api/athlete/posts/:id/like — deleteMany
POST /api/athlete/posts/:id/comments — body {content 1-1000, mentionedAthleteIds?[]}, 201 com athlete incluso
GET /api/athlete/posts/:id/comments?cursor&limit=20 — ASC por createdAt
DELETE /api/athlete/posts/:id/comments/:commentId — só autor (403 cross, 404 wrong-post)
GET /api/athlete/feed — enriched: likeCount, commentCount, likedByMe, mentionedAthletes (hydrated), metadata.tournamentName (hydrated pra TROPHY) · dedup por metadata.matchId priorizando post próprio
GET /api/athlete/search?q=&limit=8 (em routes/athlete.ts) — ILIKE nickname OR fullName, requireAuth, max 20

PostService.ts pós-Sprint 9 (backend/src/services/):

maybeCreateMatchResultPost: SEM gate hasFriendInMatch (removido). Cria post pra TODO atleta dos 4 jogadores que tem conta (resolve via team.player{1,2}Email → user → athlete). Metadata: {matchId, tournamentId, tournamentName, category, won, score, partnerAthleteId, partnerName, opponentAthleteIds[], opponentNames[], phase} (Grupo regular: phase = "Grupo {group.name}")
maybeCreatePlayoffMatchResultPost: mesma estrutura. phase = playoffPhase(roundSize) mapping: 2→"Final", 4→"Semifinal", 8→"Quartas", 16→"Oitavas"
Gate de privacy mantido (settings.matches === "PRIVATE" → pula)
Idempotência via findFirst({metadata.path["matchId"] equals matchId})
Super8Match continua sem auto-post — bug crítico Sprint 10

Frontend novos componentes (projeto_novo/src/components/):

FeedComposer.tsx — textarea controlled + MentionTextarea + contador 2000 chars + gate user.type === "ATHLETE" (cast as string)
MentionTextarea.tsx — wrap textarea/input, regex /@([\w]{0,30})$/, debounce 200ms search, top 8 results, ArrowUp/Down/Enter/Esc, inserção nickname ?? fullName.split(" ")[0] (single-word obrigatório)
AthletePostCard.tsx reescrito — sub-cards: ManualCard, MatchResultCard, TrophyCard + PostFooter (like/comment/share). renderMentions usa tokenMap com nickname ?? fullName.split(" ")[0] como chave (alinhado com inserção). TrophyCard signatura simplificada {meta} (sem content/mentionedAthletes — auto-gerado nunca tem @).

Absolute rules (não mudaram): Nunca prisma migrate dev, sempre npx prisma db push + npx prisma generate · Imports relativos (../), nunca @/ · Status enums backend MAIÚSCULO, frontend .toLowerCase() · Frontend projeto_novo/, backend backend/ · auth_user localStorage tem type valores CLUB/ATHLETE (TS type diz "club"|"athlete" — MENTIROSO, sempre cast (user.type as string)) · Login/logout sempre via useAuth(), nunca AuthService direto · Transação Prisma com Neon: { timeout: 30000 } quando 3+ creates · Banco dev = produção (mesmo Neon).
Bugs descobertos no smoke da Sprint 9 (todos no BACKLOG.md — Sprint 10):

Crítico: Super8Match não gera auto-post (carry-over Sprint 8.5) · UserType TS divergente do runtime (deveria ser "CLUB"|"ATHLETE", está "club"|"athlete") · Race 401 mount em /athlete/\* (carry-over) · Validação de gênero por categoria (carry-over)
Alta UX: AthleteProfileById back nav hardcoded "Torneios" (deveria ser context-aware) · Falta botão "Iniciar Torneio" CLOSED→ONGOING (hoje só dispara ao salvar 1º placar) · Não consigo editar placar reaberto na aba Playoffs (aba Jogos permite) · Sets adicionais não salvam (score1/score2 schema só armazena 1 set, UI multi-set é dummy) · Banner "CAMPEÕES" ausente no perfil público do torneio (só clube dono vê) · Inconsistência visual placar aba Jogos vs Playoffs (mesmo banco, exibição diferente — provavelmente inversão por viewer-team)
Média: Highlight visual de @mention no composer (precisa contentEditable refactor) · Notificação in-app de mention (depende do sistema in-app)

Estado dos dados em prod (pós-Sprint 9): Torneio novo cmqhlztsv000lr2t4eqdug48g "Smoke Test B1 — Sprint 9" criado, status ONGOING, 15 grupos, 39 group matches preenchidos (38 via seed-tournament-scores + 1 manual via UI), playoffs parciais (Open Masculino finalizou — Rafael/Sara campeões 6×3 vs Maria/H, Open Feminino e Mista incompletos). Torneios anteriores intactos. Posts MATCH_RESULT novos com metadata enriquecida; posts antigos pré-B1 (de 09-jun) renderizam com fallback graceful (sem partner/opponents/phase no metadata mas não quebra). TROPHY posts: 11 antigos (Smoke 8.5) renderizam corretamente com hidratação de tournamentName no read.
Approach refinado mid-sprint:

Prompts pro Code: 1 por turno, enxutos, sem código completo (Code escreve sozinho), sem rationale
Toda mudança espera diff antes do commit
Smoke headless (Code) + smoke visual (Rafael no browser) separados — Code não tem browser, não inventa resultados
Bug fix de produto (gate hasFriendInMatch) tratado dentro da Sprint 9 quando descoberto mid-smoke — não parqueado
Backlog atualizado em commits separados de docs (não junto com código)

Próximo: Sprint 10 — Pré-lançamento hardening. Prioridades sugeridas: (a) Super8 auto-post crítico, (b) UserType TS fix + race 401 + validação gênero, (c) UX clube (botão Iniciar Torneio + edit placar Playoffs + sets múltiplos + banner público), (d) UX feed (highlight composer + notificações mention). Abrir Gate 0 de planejamento em chat novo.
